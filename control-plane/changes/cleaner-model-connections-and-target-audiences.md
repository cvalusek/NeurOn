# Cleaner model connections and target audiences

- Simplified reservation connection cards to one short alias, one target alias,
  and one model ID, with direct-host aliases shown consistently.
- Replaced guessed LiteLLM dashboard paths with an explicit `LITELLM_UI_URL` and
  compact launch icons for external destinations.
- Derived dedicated versus multi-model hosting from the discovered model count.
- Replaced raw target-audience ID fields with conditional team and user choices.
