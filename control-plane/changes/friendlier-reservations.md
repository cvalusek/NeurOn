# Friendlier reservations and profiles

Home now manages every active reservation, profile timing stays visibly synchronized, and first-time users get a short guide. Profiles can combine multiple target-and-model selections, with automatic selection for single-model targets. EC2 reservations wait safely while an instance finishes stopping, and traffic-tail cost stays with the real reservations that participated in the activation.
