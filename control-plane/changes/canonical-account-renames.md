# Safely rename canonical accounts

Administrators can now rename an active account from the Accounts screen. The
transaction preserves provider identities, updates owned-record labels, revokes
old sessions, and can reclaim a username from that account's disabled merged
alias without manual database edits.
